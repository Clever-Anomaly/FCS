After cloning repo:

cd to the directory of repo and create a python virtual environment and then activate it

then cd to backend directory and use: pip install -r requirements.txt

then cd to frontend directory and run: npm install

then installment is done:

for running backend cd to backend directory and run: python manage.py runserver

for running frontend cd to frontend directory and run: npm run dev
